# =============================================================================
# Grafana Configuration for MoaV Monitoring
# =============================================================================

[server]
http_port = 3000

[security]
admin_user = admin
# Password set via GF_SECURITY_ADMIN_PASSWORD env var

[auth.anonymous]
enabled = false

[users]
allow_sign_up = false

[dashboards]
default_home_dashboard_path = /var/lib/grafana/dashboards/system.json

[log]
mode = console
level = warn
