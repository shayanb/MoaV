# TrustTunnel TLS Hosts Configuration
# Generated by MoaV bootstrap

[[hosts]]
hostname = "${DOMAIN}"
cert_chain_path = "/certs/live/${DOMAIN}/fullchain.pem"
private_key_path = "/certs/live/${DOMAIN}/privkey.pem"
